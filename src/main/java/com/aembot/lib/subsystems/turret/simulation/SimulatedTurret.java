package com.aembot.lib.subsystems.turret.simulation;
// TODO uncomment and finish this
/*package com.aembot.lib.subsystems.base.turret.simulation;

import org.littletonrobotics.junction.Logger;

import com.ctre.phoenix6.configs.TalonFXConfiguration;
import com.ctre.phoenix6.signals.InvertedValue;
import com.ctre.phoenix6.sim.ChassisReference;
import com.ctre.phoenix6.sim.TalonFXSimState;
import com.aembot.lib.config.motors.MotorFollowersConfiguration;
import com.aembot.lib.config.subsystems.turret.simulation.SimulatedTurretConfiguration;
import com.aembot.lib.core.logging.Loggable;
import com.aembot.lib.core.motors.interfaces.MotorIO.FollowDirection;
import com.aembot.lib.core.motors.io.MotorIOTalonFX;
import com.aembot.lib.core.motors.io.MotorIOTalonFXSim;
import com.aembot.lib.subsystems.base.turret.visualizations.TurretVisualizer;

import edu.wpi.first.math.system.plant.DCMotor;
import edu.wpi.first.wpilibj.Notifier;
import edu.wpi.first.wpilibj.RobotController;
import edu.wpi.first.wpilibj.Timer;
//import edu.wpi.first.wpilibj.simulation.ElevatorSim;
// conversion pattern/ratio from degrees to smartvelocity thing

import edu.wpi.first.math.MathUtil;
import edu.wpi.first.math.Matrix;
import edu.wpi.first.math.VecBuilder;
import edu.wpi.first.math.numbers.N1;
import edu.wpi.first.math.numbers.N2;
import edu.wpi.first.math.system.LinearSystem;
import edu.wpi.first.math.system.NumericalIntegration;
import edu.wpi.first.math.system.plant.DCMotor;
import edu.wpi.first.math.system.plant.LinearSystemId;
import edu.wpi.first.wpilibj.RobotController;
//linearid.createdcmotor
//dc motor sim type
// use dcmotor sim type, linear system id.create that instead of whatever when it asks for it

//write class or whatever. TurretSim like wpilib ElevatorSim.
//write the function to use in RobotContainer in subsystem. TurretSubsystem .
// TODO rewrite the turret files to use MotorSubsystem instead of MotorFollower
public class SimulatedTurret implements Loggable{

    class Simulated
}
*/
